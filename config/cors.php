<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Paths that should be accessible via CORS
    |--------------------------------------------------------------------------
    |
    | المسارات التي تريد السماح بالوصول إليها من دومينات أخرى (Cross-Origin).
    | عادةً تضع هنا مسارات الـ API أو نقاط النهاية التي تحتاجها.
    |
    */
    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Origins
    |--------------------------------------------------------------------------
    |
    | قائمة الدومينات التي يسمح لها بالوصول إلى الـ API.
    | أثناء التطوير يمكنك وضع '*' للسماح لكل الدومينات.
    | في بيئة الإنتاج من الأفضل تحديد الدومين الخاص بتطبيقك.
    |
    */
    'allowed_origins' => ['laravel-my-own-website.railway.internal'], // أو ['https://your-railway-domain.railway.app']

    /*
    |--------------------------------------------------------------------------
    | Allowed Methods
    |--------------------------------------------------------------------------
    |
    | HTTP Methods المسموح بها. عادةً تستخدم ['*'] للسماح بكل الطرق.
    |
    */
    'allowed_methods' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Headers
    |--------------------------------------------------------------------------
    |
    | رؤوس HTTP المسموح بها في الطلبات القادمة.
    |
    */
    'allowed_headers' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Exposed Headers
    |--------------------------------------------------------------------------
    |
    | رؤوس HTTP التي تريد إرسالها في الاستجابة للسماح بالوصول إليها من المتصفح.
    |
    */
    'exposed_headers' => [],

    /*
    |--------------------------------------------------------------------------
    | Max Age
    |--------------------------------------------------------------------------
    |
    | مدة تخزين سياسة CORS في المتصفح (بالثواني).
    | تقليلها أثناء التطوير مفيد.
    |
    */
    'max_age' => 0,

    /*
    |--------------------------------------------------------------------------
    | Supports Credentials
    |--------------------------------------------------------------------------
    |
    | هل تريد السماح بإرسال الكوكيز أو بيانات الجلسة مع طلبات CORS.
    | إذا كان تطبيقك يحتاج تسجيل دخول من متصفحات مختلفة، فعّلها.
    |
    */
    'supports_credentials' => false,

];
