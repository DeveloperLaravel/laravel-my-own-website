{
  "build": {
    "commands": [
      "composer install --no-interaction --prefer-dist",
      "npm install",
      "npm run build"
    ]
  },
  "start": {
    "command": "php artisan serve --host=0.0.0.0 --port=8080"
  }
}
